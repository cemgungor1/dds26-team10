bind 0.0.0.0
protected-mode no
port 6379

requirepass redis
masterauth redis

dir /data
appendonly yes
appendfsync everysec
appendfilename "appendonly.aof"

timeout 0

# Disable RDB snapshots and AOF on replicas (they don't need durability)
save ""